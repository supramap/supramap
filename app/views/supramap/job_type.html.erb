<fieldset><legend>Define new job</legend>
	
	<% form_for :job, @job, :url => {:action => "add_job"} do |form| %>
		<table><tr>
			<tr>
				<td>New job  <%= form.text_field :name, :size => 20 %></td>
				<input type="hidden" id="user_id" name="job[project_id]" value="<%= h(@project.id) %>" />
			</tr>
			<tr>
				<!-- #form.select :job_type, ["fas", "xml"]-->
				<input type="hidden" id="user_id" name="job[job_type]" value="fas" />
			</tr>
			<tr>
				<td><%= submit_tag "Define", :class => "submit" %></td></tr>
			</tr>
		</table>
	<% end%>
